#!/bin/sh

# check_dir () {
#     if [ "$setting" != "in" -a "$setting" != "out" ]; then
#         read -p "(i)n or (o)ut? " arg
#         case $arg in
#             i) dir=in ;;
#             o) dir=o ;;
#             *) check_dir ;;
#         esac
#     fi
# }

# dir="$1"
# check_dir

echo "dotfiles: "
push-dot-repo
echo
echo "scripts: "
push-repo $HOME/scripts 
echo
echo "notes: "
push-repo $HOME/notes
echo
echo "passwords: "
push-repo $HOME/.passwords
echo
echo "todo: "
push-repo $HOME/todo
