#!/bin/sh


dir="$PWD"
cd

if [ "$1" = pull ]
then
	git --git-dir=$HOME/.dot.git/ --work-tree=$HOME pull
	git pull
	cd notes
	git pull
else
	sync-repo-dot
	sync-repo
	cd notes
	sync-repo
fi

cd "$dir"
